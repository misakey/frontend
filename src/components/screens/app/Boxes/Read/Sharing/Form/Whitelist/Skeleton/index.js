import React from 'react';
import Skeleton from '@material-ui/lab/Skeleton';

const SharingFormWhitelistSkeleton = (props) => (
  <Skeleton height={72} {...props} variant="rect" />
);

export default SharingFormWhitelistSkeleton;
