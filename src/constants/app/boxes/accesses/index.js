import { IDENTIFIER, EMAIL_DOMAIN } from '@misakey/ui/constants/accessTypes';

export const INVITATION_LINK = 'invitation_link';

export const LIMITED_RESTRICTION_TYPES = [
  IDENTIFIER,
  EMAIL_DOMAIN,
];

export const RESTRICTION_TYPES = {
  INVITATION_LINK,
  IDENTIFIER,
  EMAIL_DOMAIN,
};

