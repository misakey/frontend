import { IDENTIFIER, EMAIL_DOMAIN } from '@misakey/core/api/constants/accessTypes';

export const LIMITED_RESTRICTION_TYPES = [
  IDENTIFIER,
  EMAIL_DOMAIN,
];

export const RESTRICTION_TYPES = {
  IDENTIFIER,
  EMAIL_DOMAIN,
};
